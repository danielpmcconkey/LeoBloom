# Leo Bloom — Wakeup Notes (2026-04-02c)

## Status

Project 1 merged. Project restructure merged. Project 2 BRD written. No
implementation started on Project 2 yet.

## What happened this session

- Installed .NET 10 on host (10.0.201, ~/.dotnet)
- Designed COA through 4 revisions — property/personal split at every account
  type, 7xxx split into 7100 Other Income / 7200 Other Expense
- Real COA: `~/penthouse-pete/property/ChartOfAccountsDraft.md` (v4)
- Anonymized sample COA: `Specs/SampleCOA.md`
- BD built Project 1 (migrations, schema, seed data) on feature branch
- Dan reviewed and merged PR #1 via GitHub UI (learning git/PR workflow)
- Ran BD's migrations against prod, overwrote sample COA with real names
  - Overwrite script: `~/penthouse-pete/property/prod-coa-overwrite.sql`
- Restructured solution: Ledger + Ops + Data → Domain + Dal
  - `LeoBloom.Domain` — pure types (Ledger.fs, Ops.fs, F# file ordering)
  - `LeoBloom.Dal` — data access + shared ConnectionString resolver
  - `LeoBloom.Domain.Tests` — business logic BDD (empty, future)
  - `LeoBloom.Dal.Tests` — structural constraint Gherkin tests
- Dan reviewed and merged PR #2 via GitHub UI
- Added `LEOBLOOM_ENV=Production` to Dan's .bashrc
- Wrote Project 002 BRD (test harness + config + prod safety)
- Renumbered projects: Project1 → Project001, new is Project002

## What's next — PRIORITY

**Dan is not satisfied with the data safety story.** This is the first
conversation topic next session. Before handing Project 2 to BD, resolve:

- How do we prevent BD's migrations from overwriting prod COA names?
- Migration tagging (dev-only vs shared)?
- Should Hobson review every migration before it runs against prod?
- The current gate is Postgres role permissions (claude can't write to
  leobloom_prod). Is that sufficient or do we need more?
- Dan's concern: "I don't want BD overwriting our prod database when he
  accidentally puts something in a migration that we don't notice."

After data safety is resolved, hand Project 2 to BD with a wakeup note
that covers the restructure.

## Databases

- `leobloom_prod` on host Postgres. Role: `dansdev`. Migrated, real COA.
- `leobloom_dev` on host Postgres (BD accesses via Docker bridge). Role: `claude`. Migrated, sample COA.

## Key decisions made

- Two databases, one Postgres instance
- `LEOBLOOM_ENV` drives config (Production / Development)
- appsettings.{env}.json for connection strings, env vars for secrets
- F# all the way down
- Domain + Dal project structure (not Ledger + Ops + Data)
- Shared ConnectionString resolver in Dal
- No versions in filenames — git handles versioning
- Projects numbered 001, 002, etc.
- Dan learning git/PR workflow — wants to review PRs like a team lead

## Repo structure (current)

```
LeoBloom/
├── LeoBloom.sln
├── Specs/
│   ├── DataModelSpec.md
│   ├── SampleCOA.md
│   └── Acceptance/
│       ├── LedgerStructuralConstraints.feature
│       └── OpsStructuralConstraints.feature
├── Projects/
│   ├── Project001-Database.md
│   └── Project002-TestHarness.md
├── HobsonsNotes/
├── BdsNotes/
├── Src/
│   ├── LeoBloom.Domain/
│   ├── LeoBloom.Domain.Tests/
│   ├── LeoBloom.Dal/
│   ├── LeoBloom.Dal.Tests/
│   ├── LeoBloom.Migrations/
│   └── LeoBloom.Api/
└── .gitignore
```

## Files outside repo

- `~/penthouse-pete/property/ChartOfAccountsDraft.md` — real COA (v4)
- `~/penthouse-pete/property/prod-coa-overwrite.sql` — name overwrite script

## Dan action items (still open)

- HOA billing details (unknown)
- Closing docs from wife's Google Drive (property tax proration)
- CPA depreciation schedule (2026 tax filing)

## Tell BD

When waking BD for Project 2, he needs to know:
- Solution restructured: Domain + Dal replaces Ledger + Ops + Data
- His test projects are now Dal.Tests and Domain.Tests
- ConnectionString resolver exists in Dal — use it, don't duplicate
- `LEOBLOOM_ENV` is the environment selector
- Review Project002-TestHarness.md for full scope

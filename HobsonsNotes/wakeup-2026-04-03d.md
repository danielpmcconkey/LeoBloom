# Hobson Wakeup — 2026-04-03d

## Status

Migration prod governance tooling built but untested. Session ended before
the env var was live.

## What happened this session

- Took on PO role, then got corrected to **Comptroller**. BD owns the product.
  Dan is exec. Hobson is the customer and prod operator. See `Projects/Workflow.md`.
- Wrote the full product backlog (`Projects/BACKLOG.md`) — 10 epics, 23 stories,
  Projects 005–027. Each story has a full brief for CE to expand into BRDs.
- Reviewed and approved BD's Project 3 BRD and BDD doc. Project 3 done and merged.
- Reviewed BD's Project 4 BRD (domain types). Also done and merged.
- BD built Project 029 (lookup table elimination) — merged to main.
- Built migration prod governance tooling:
  - `~/penthouse-pete/leobloom-ops/review-migration.sh` — review phase
  - `~/penthouse-pete/leobloom-ops/execute-migration.sh` — execute phase
  - `~/.claude/hooks/guard.sh` — updated to block direct dotnet run on Migrations against prod
- Created `leobloom_hobson` Postgres role scoped to leobloom_prod only.
- Updated `appsettings.Production.json` to use `leobloom_hobson` instead of `dansdev`.
- Wrote migration-prod-executor agent blueprint in ai-dev-playbook.
- Fixed hook false positive — was triggering on git commands mentioning the path.

## What's next — PRIORITY

**Test the migration governance tooling end-to-end.**

Migration 19 (`1712000019000_EliminateLookupTables.sql`) is pending in prod.
This is the lookup table elimination — replaces FK int columns with varchar,
drops 4 lookup tables. BD already ran it in dev.

1. Verify `LEOBLOOM_DB_PASSWORD` env var is live (Dan added it to `.bashrc`,
   needs a session restart to pick it up).
2. Run `~/penthouse-pete/leobloom-ops/review-migration.sh` — should show only
   migration 19 as pending (the first 18 were already applied to prod).
3. Review the SQL, approve, verify artifact is written.
4. Run `~/penthouse-pete/leobloom-ops/execute-migration.sh` — verify hash
   match, migration applies, artifact is deleted.
5. Test the hook: try to run `dotnet run --project Src/LeoBloom.Migrations/`
   directly — should be blocked.

## Known issue

Last test run of `review-migration.sh` showed 19 pending migrations instead
of 1. Either the Migrondi journal table is empty/missing in prod, or the
query isn't matching the journal entries correctly. Investigate before
executing — if all 18 are re-run, they'll fail on existing tables.

## Key paths

- Migration scripts: `/media/dan/fdrive/codeprojects/LeoBloom/Src/LeoBloom.Migrations/Migrations/`
- Governance tooling: `~/penthouse-pete/leobloom-ops/`
- Hook: `~/.claude/hooks/guard.sh`
- Agent blueprint: `/media/dan/fdrive/codeprojects/ai-dev-playbook/Tooling/DansAgents/migration-prod-executor.md`
- Workflow doc: `/media/dan/fdrive/codeprojects/LeoBloom/Projects/Workflow.md`
- Backlog: `/media/dan/fdrive/codeprojects/LeoBloom/Projects/BACKLOG.md`

## Databases

- `leobloom_prod` — host Postgres. Role: `leobloom_hobson`. Password in `LEOBLOOM_DB_PASSWORD` env var.
- `leobloom_dev` — BD via Docker bridge. Role: `claude`. Password: `claude`.

## Memory updates this session

- LeoBloom project memory updated with new team roles
- Migration review feedback saved
- PO decision authority feedback rewritten (Dan relays, not a message bus)

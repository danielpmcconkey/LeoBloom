# Hobson Wakeup — 2026-04-04a

## Status

Production DB populated with obligation agreements and instances. Invoice
generator rewritten to pull from DB. Migration governance tooling confirmed
working (all 19 migrations applied, env var live).

## What happened this session

- Verified `LEOBLOOM_DB_PASSWORD` env var is set (20 chars).
- Confirmed all migrations applied to prod — no pending.
- Inserted 9 obligation agreements into `ops.obligation_agreement`:
  - 3 receivables: Brian rent+utils, Alex rent+utils, Justin utils-only
  - 6 payables: Mortgage (Rocket), Water/Electric (City of Concord),
    Gas (Enbridge), Insurance (Allstate), HOA (Sheffield Manor), Property Tax (Cabarrus County)
- Inserted 12 obligation instances into `ops.obligation_instance`:
  - Feb tenant receivables: Brian in_flight, Alex confirmed, Justin overdue
  - Mar tenant receivables: all expected (newly created this session)
  - Mortgage: Mar confirmed (SECU), Apr confirmed (CMA autopay)
  - Water/Electric: Feb confirmed, Mar in_flight
  - Gas: Feb confirmed, Mar expected (autopay Apr 6)
  - Insurance: 2026 confirmed
  - HOA: Feb confirmed, May expected
- Rewrote `generate_bills.py` to pull from leobloom_prod instead of spreadsheet.
  - Fixed billing period mapping (instance `name` field, not `expected_date`)
  - Added utility total line
  - Updated payment instructions to Zelle/Ally
  - Records invoices in `ops.invoice` table (upsert)
- Created March receivable instances with correct amounts.
- Used `tri_annual` cadence for HOA — not in the F# DU yet. BD needs to add
  `TriAnnual` case to `RecurrenceCadence`.
- Reviewed Project 028 (Write-Level Ledger Validation): BD's assessment is
  correct — 005 and 006 integration tests already cover the full scope.
  Every test calls `JournalEntryService.post`/`voidEntry` through real DB
  connections. Recommend marking 028 "completed by prior work."

## What's next

- **March invoices sent** (done on property side — see property hobsons_notes).
- **BD backlog item:** Add `TriAnnual` case to `RecurrenceCadence` DU + fromString/toString.
- **Project 028 decision:** Dan reviewing tomorrow. Hobson recommends "completed by prior work."
- **Account balance queries (Project 007)** — next epic once Dan's ready.

## Known issues

- `dest_account_id` on tenant receivable agreements points to Ally Checking (id 4).
  Payment methods changing soon per Dan. Update when settled.
- Gas autopay pulling from wrong account (personal Fidelity Brokerage x6967).
  Dan aware, needs to change at Enbridge.
- Alex's Feb payment landed in personal Ally (x3700) — wrong Zelle default.
  Dan fixed the default account. Future Zelle payments should land in business Ally.

## Key paths

- Invoice generator: `/home/dan/penthouse-pete/property/generate_bills.py`
- Invoice output: `/home/dan/penthouse-pete/property/invoices/`
- Data model spec: `/media/dan/fdrive/codeprojects/LeoBloom/Projects/Project001-Database/DataModelSpec.md`
- Backlog: `/media/dan/fdrive/codeprojects/LeoBloom/Projects/BACKLOG.md`
- Domain types: `/media/dan/fdrive/codeprojects/LeoBloom/Src/LeoBloom.Domain/Ops.fs`

## Databases

- `leobloom_prod` — host Postgres. Role: `leobloom_hobson`. Password in `LEOBLOOM_DB_PASSWORD` env var.
- `leobloom_dev` — BD via Docker bridge. Role: `claude`. Password: `claude`.

# Wakeup — 2026-04-05j

## What happened this session

### P045 — Domain-Based Project Reorganization (shipped)
Full agent pipeline. PR #22 merged. Split LeoBloom.Utilities (24 Compile includes) into three projects:
- **LeoBloom.Utilities** — 2 files (Log.fs, DataSource.fs)
- **LeoBloom.Ledger** — 15 files (ledger services/repos)
- **LeoBloom.Ops** — 7 files (ops services/repos)

New fsproj files created, namespaces updated, test imports updated, solution file updated. FT-DUR-005 test updated to reflect new Utilities contents. 428 tests passing.

### P046 + P047 — Delete Api + Ghost Directories (shipped)
No pipeline — housekeeping tasks. PR #23 merged. Removed LeoBloom.Api (dead WeatherForecast template), LeoBloom.Data, LeoBloom.Ledger.Tests, LeoBloom.Ops.Tests (empty scaffolding). Deleted FT-LI-001 test (tested deleted Api). 427 tests.

### P050 + P051 — EntryType.toDbString + IncludeErrorDetail (shipped)
No pipeline — trivial one-liners. PR #24 merged. Replaced inline match with `EntryType.toDbString` in JournalEntryRepository. Moved `IncludeErrorDetail` from hardcoded DataSource.fs to dev appsettings connection strings. 427 tests.

### P048 — Test Cleanup (shipped)
Full agent pipeline. PR #25 merged. Removed 26 dead tests (tautologies, ghost guards, architectural assertions) across 4 files. Deleted DalToUtilitiesRenameTests.fs entirely. Trimmed 3 other test files and 4 feature files. Added verified-by-design docs to P031 and P033 test results. 401 tests.

### Workflow/memory updates
- New feedback memory: agents are Agent tool subagent types, DSWF files are project-specific addenda (not agent definitions).

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Remaining remediation
1. **P049 — Consolidate helpers** — extract `optParam` into DataHelpers.fs, rewrite `repoRoot` with `[<CallerFilePath>]`, consolidate constraint test helpers. Last remediation item.

### Then CLI track (in order)
1. P036 — CLI framework + ledger commands (everything else depends on this)
2. P037, P038, P039, P041 — thin CLI wrappers, any order
3. P021 → P042 — invoice persistence then CLI wrapper
4. P040 — CLI tax reports (new logic, target before 2027 tax season)
5. P035 — orphaned posting detection (standalone)
6. P022 — balance projection (lowest priority)

## Current state

### Branch: main
HEAD: `e77b386`
Working tree: clean (untracked wakeup files only)
Pushed to origin: yes

### Test suite
401 tests, all passing.

### Backlog snapshot
- Done: P001-P019, P029-P034, P043-P048, P050-P051
- Cancelled: P020, P023-P027
- Not started: P021, P022, P035-P042, P049

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`

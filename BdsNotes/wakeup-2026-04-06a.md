# Wakeup — 2026-04-06a

## What happened this session

Four projects shipped through the full pipeline (PO → Gherkin → Builder → QE → Reviewer → Governor → PO signoff → RTE merge).

### P053 — Fix Pre-Existing Test Failures (done, merged)

- Moved all 19 PostObligationToLedgerTests dates from 2026 to 2099 to fix seed data collision
- Added comment block documenting the convention
- **PR #30**, merged to main

### P054 — Seed Data Separation (done, merged)

- Created `Src/LeoBloom.Migrations/Seeds/dev/` with two idempotent SQL scripts:
  - `010-fiscal-periods.sql` (36 periods, upsert)
  - `020-chart-of-accounts.sql` (69 accounts, upsert with account_subtype)
- Created `Seeds/run-seeds.sh` (bash runner, psql, sorted execution, error handling)
- Created `Seeds/README.md`
- 7 new tests in SeedRunnerTests.fs
- **PR #31**, merged to main

### P021 — Invoice Persistence (done, merged)

- `RecordInvoiceCommand` type + `InvoiceValidation` module in Domain/Ops.fs
- `InvoiceRepository.fs` (insert, findById, findByTenantAndPeriod, list with filter)
- `InvoiceService.fs` (recordInvoice, showInvoice, listInvoices)
- Test infrastructure: insertInvoice helper, trackInvoice, cleanup ordering
- 24 new tests in InvoiceTests.fs
- Design decisions: closed periods allowed (source doc not posting), generated_at from caller, reject-don't-round on precision, dual duplicate detection
- **PR #32**, merged to main

### P042 — CLI Invoice Commands (done, merged)

- `InvoiceCommands.fs` — Argu DUs, handlers for record/show/list, dispatch
- `OutputFormatter.fs` ��� formatInvoice, writeInvoiceList (solved type erasure via dedicated typed function)
- `Program.fs` — Invoice case in top-level DU
- fsproj — Ops reference added to CLI project
- 19 new tests in InvoiceCommandsTests.fs
- **PR #33**, merged to main

### P055 — Closed Period Posting Guard (backlog item created)

- 3 pre-existing test failures in PostObligationToLedgerTests assert closed-period rejection behavior that doesn't exist yet
- Backlog item created as **Blocked (design decision)** — Dan needs to answer 5 design questions before this enters the pipeline
- Brief at Projects/Project055-ClosedPeriodPostingGuard/Project055-brief.md

### Workflow update

- Dan added orchestrator autonomy instruction to workflow.md line 11: BD makes minor decisions using GAAP/SDLC, only stops for real-consequence decisions

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

1. **P055 — Closed Period Posting Guard** — blocked on Dan's design input (5 questions in brief). Don't start until he gives direction.
2. **P037/038/039/041** — CLI wrappers (transfers, reports, etc.) in any order. These are independent mechanical wrappers like P042.
3. **P022, P035** — remaining backlog items, lower priority.

## Current state

### Branch: main
HEAD: `227cba9`
Working tree: clean.

### Test suite
655 passed, 3 failed, 0 skipped (658 total).

Failures (all pre-existing, tracked as P055):
- `PostObligationToLedgerTests.posting when fiscal period is closed returns error`
- `PostObligationToLedgerTests.failed post to closed period leaves instance in confirmed status with no journal entry`
- `TransferTests.Journal entry has transfer source and reference`

### Backlog snapshot
- Done: P001-P019, P021, P029-P034, P036, P040, P042-P054
- Cancelled: P020, P023-P027
- Blocked (design decision): **P055**
- Not started: P022, P028, P035, P037-P039, P041

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`

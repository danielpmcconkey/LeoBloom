# Wakeup — 2026-04-05m

## What happened this session

### P052 — Account Sub-Type Classification (done, merged)

Full pipeline: Gherkin → Builder → QE → Reviewer → Governor → PO → RTE.

- Added `account_subtype varchar(25)` nullable column to `ledger.account`
- `AccountSubType` DU with 9 cases, validation, string converters
- Seed data classified (Cash, FixedAsset, Investment, CurrentLiability, etc.)
- Phases 3 & 4 (Ledger/Ops repo updates) intentionally skipped — no existing code constructs the `Account` domain type. Reviewer and Governor confirmed.
- 89 new tests (546 total passing on main before P040 merge)
- **PR #28**, merged to main

### P040 — CLI Tax Reports (done, merged)

Resumed from prior session (was paused on P052 blocker). Pipeline picked up at reviewer fix #6.

- Fixed CashFlowRepository: replaced hardcoded account codes (`1110`, `1120`, `1210`, `1220`) with `WHERE account_subtype = 'Cash'`
- This correctly excludes 1210 (Brokerage = Investment, not Cash)
- Fixed test isolation: `createReportingEnv` now uses seed accounts instead of inserting duplicates that collide with seed data
- Fixed stale assertion: account 1110 name is "Bank A — Operating" not "Rental Checking"
- Governor → PO → RTE, all approved
- **PR #29**, merged to main

### P053 — Fix Pre-Existing Test Failures (story created)

Dan called out that tolerating 5 pre-existing failures is unacceptable. PO investigated root cause:

**Not bad test data.** Test isolation bug — seed migration populates fiscal periods for 2026-2028, and 5 `PostObligationToLedgerTests` create their own periods for colliding date ranges. `findByDate` returns the seed period instead of the test period.

Brief written, backlog updated, high priority — slotted before new feature work.

### P054 — Seed Data Separation (story created)

Hobson wrote a vision doc (`BdsNotes/seed-data-vision-2026-04-05.md`) identifying that seed data is baked into the migration chain. Dev and prod get identical data, which is wrong — prod has real account names that Hobson maintains. Migration 021 (P052) is already an example: its UPDATE statements target anonymized seed accounts but prod has a different COA structure.

Solution: `Seeds/` directory with idempotent env-specific scripts, seed runner, retrofit existing migrations. High priority infrastructure — sequenced after P053.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

1. **Execute P053** — fix the 5 PostObligationToLedger test failures. High priority.
2. **Execute P054** — separate seed data from migrations. Read `BdsNotes/seed-data-vision-2026-04-05.md` for the full vision.
3. After foundation cleanup: P037/038/039/041 (CLI wrappers) in any order, or P021 → P042 (invoice persistence + CLI).

## Current state

### Branch: main
HEAD: `f974ec7`
Working tree: clean. No uncommitted changes.

### Test suite
602 passed, 6 failed (PostObligationToLedgerTests — P053 will fix these).

### Backlog snapshot
- Done: P001-P019, P029-P034, P036, P040, P043-P052
- Cancelled: P020, P023-P027
- Not started (high priority): **P053, P054**
- Not started: P021, P022, P035, P037-P039, P041-P042

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`

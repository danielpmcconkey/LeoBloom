# Wakeup — 2026-04-03c (Project 4 Prep)

## What happened this session

Closed out Projects 2 and 3. Built the agent blueprint system. This was
a big session — read carefully.

### Project 2: Closed
- Branch `feat/project2-test-harness` deleted (was already merged to main)
- BACKLOG.md status updated to Done
- 88/88 tests confirmed passing

### Project 3: Built and Merged
- All 8 deliverables shipped, 29/29 BDD criteria verified
- Feature files moved to `Specs/Structural/` (pure Gherkin, nothing else)
- DataModelSpec.md and SampleCOA.md moved to `Projects/Project001-Database/`
- 88 `@FT-*` tags applied (LSC-001→029, OSC-001→041, DR-001→018)
- DeleteTarget refactor: 12 When steps → 1 (`I delete the parent record`)
  via `withTarget` helper and `DeleteTarget` type on `ScenarioContext`
- `Trait("Category", "Structural")` on FeatureFixture.fs
- `Documentation/README.md` routing table created
- `.fsproj` paths updated
- Test results written for Projects 1 and 3
- Hobson reviewed and approved. Merged to main, branch cleaned up.

### Design decisions made this session

1. **Specs/ is pure Gherkin.** Nothing else. Design docs are project artifacts.
2. **No retroactive BDD docs** for Projects 1-2. Flow starts clean from 3.
3. **Documentation/README.md** is a folder routing table, not docs.
4. **.feature files are enforced specs.** .md files are not. If it's not
   executable, it's not in Specs/.

### Agent blueprints built

Dan defined a development pod with 7 roles. Blueprints live in
`/workspace/ai-dev-playbook/Tooling/DansAgents/` and are symlinked to
`~/.claude/agents/`. Each has Claude Code frontmatter (name, description,
tools) for subagent spawning.

| Agent | File | Tools |
|---|---|---|
| PO | `po.md` | Read, Glob, Grep |
| BA | `ba.md` | Read, Write, Glob, Grep, Bash, Skill, Agent |
| Planner | `planner.md` | Read, Write, Glob, Grep, Bash, Skill, Agent |
| Builder | `builder.md` | Read, Write, Edit, Glob, Grep, Bash, Skill, Agent |
| Reviewer | `reviewer.md` | Read, Glob, Grep, Bash, Skill, Agent |
| Governor | `governor.md` | Read, Write, Glob, Grep, Bash |
| RTE | `rte.md` | Read, Glob, Bash |

Conventions file: `_conventions.md` (outcome enum, evidence rules, traceability).

### Role changes

- **BD (you) is now the Product Owner.** Not just tech lead — you own the
  product. Hobson is the customer, not the PO.
- **Hobson is Comptroller.** He enters data in prod, reviews finances,
  and has the Migration Prod Executor skill.
- **Dan is exec.** Vision, strategy, approval on big calls.
- **CE is the toolchain** baked into agent blueprints (BA uses brainstorm/plan,
  Builder uses work, Reviewer uses review).

### Workflow (from `Projects/Workflow.md`)

```
PO picks backlog item → PO creates project dir → RTE creates branch →
BA writes BRD → PO approves → BA writes BDD → PO approves →
Planner creates plan → Builder builds → Reviewer reviews →
Dan/BD take findings case-by-case → Governor writes test results →
PO signs off → RTE merges
```

## What's next: Project 4

From the backlog:

> **004** | Domain types — F# types in Domain, business logic BDD in Domain.Tests | Backlog

This is the first project to use the full agent workflow. The agents
should be spawned as subagents to keep your context lean.

### First-time workflow checklist

Before spawning agents, verify:
- [ ] Symlinks still exist: `ls -la ~/.claude/agents/{po,ba,planner,builder,reviewer,governor,rte}.md`
- [ ] If missing: `ln -sf /workspace/ai-dev-playbook/Tooling/DansAgents/*.md ~/.claude/agents/`
- [ ] The agents haven't been tested in a real workflow yet. Expect friction.
  Be ready to adjust blueprints as you discover what works and what doesn't.

### What Project 4 probably involves

- F# types in `LeoBloom.Domain` mirroring the database schema
- Record types for: AccountType, Account, FiscalPeriod, JournalEntry,
  JournalEntryReference, JournalEntryLine, ObligationType, ObligationStatus,
  Cadence, PaymentMethod, ObligationAgreement, ObligationInstance, Transfer, Invoice
- BDD scenarios in `LeoBloom.Domain.Tests` for domain logic
- The Domain.Tests project already exists with 1 passing test (placeholder)

But don't assume — let the BA research and write the BRD. That's the flow.

## Environment

- .NET 10: `/home/sandbox/.dotnet`
- `export DOTNET_ROOT=/home/sandbox/.dotnet && export PATH=$DOTNET_ROOT:$PATH`
- `export LEOBLOOM_ENV=Development && export LEOBLOOM_DB_PASSWORD=claude`
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, password `claude`, db `leobloom_dev`

# Wakeup — 2026-04-07b

## What happened this session

### RCA on Nightshift test failures

Nightshift marked P058/P059/P061 complete, but the test suite had failures. Ran full RCA:

**Deterministic failure (fixed):** SeedRunnerTests FT-SR-008 and FT-SR-011 hardcoded `SELECT COUNT(*) FROM portfolio.tax_bucket` expecting 5. Portfolio test classes running in xUnit parallel insert temporary tax buckets, inflating the count. Fixed by scoping the query to `WHERE name IN (...)` for the 5 known seed names. Committed in `2fdde8c`.

**Intermittent failures (root-caused, not fixed):** ~1 in 3-4 full suite runs drops a random single test. Different test each time, always passes in isolation. Root cause: services own their own connections (`DataSource.openConnection()` inside every service function). Tests must commit setup data for the service to see it, making test data globally visible to parallel tests. Full RCA in `BdsNotes/rca-test-isolation-2026-04-07.md`.

### Hobson's GAAP audit

Hobson ran 9 auditors across the full codebase. Results in `HobsonsNotes/GaapAssessment-2026-04-07/`. Key findings:

- Math is clean (89 scenarios, zero discrepancies)
- Normal balance formula duplicated in 7+ places (most dangerous duplication)
- Non-atomic multi-phase writes (same root cause as test isolation — services own connections)
- Portfolio validation gaps, missing CLI commands, fiscal period overlap unguarded

### New backlog (062–073)

Hobson wrote 062–072 from audit findings. I wrote 073 (connection injection / test isolation). All 12 cards queued in Nightshift DB, P073 unblocked at priority 1, everything else blocked on it.

### Nightshift blueprint edits

Clarified test responsibility: QE is the single owner of full suite execution and evidence (`test-results.md` artifact). Builder runs scoped tests only. Reviewer and Governor verify QE's artifact instead of re-running. Committed in Nightshift `0ac5024`.

### Engine restarted

Nightshift v0.1.1 started, picked up P073 immediately. Running in Dan's terminal.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

1. **Monitor P073 progress** — this is the big one. Every service function signature changes.
2. **After P073:** 11 cards auto-unblock. Engine will process by priority.
3. **Backlog index is stale** — P022/P035/P038/P056/P057/P058/P059/P061 should be marked Done, 062–073 should be added.
4. **P060** — Hobson's data migration. Still not a Nightshift card.

## Current state

### Branch: main
- HEAD: `2fdde8c` fix: scope seed runner tax bucket assertions, add P073 backlog spec
- Working tree: clean

### Nightshift queue
- Card 9 (P073): in_progress (rte_setup step)
- Cards 10–20 (P062–P072): queued, all blocked on card 9

### Test suite
- 973 tests, 973 passing on clean runs
- Intermittent parallel failures (~1 in 3-4 runs) until P073 lands

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Nightshift DB: `172.18.0.1:5432`, user `nightshift`, db `nightshift`

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-07b.md in the LeoBloom repo and report back
```

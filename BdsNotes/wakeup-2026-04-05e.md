# Wakeup — 2026-04-05e

## What happened this session

### Project 018 — Post Obligation to Ledger shipped
- Convergence point: ops meets ledger. Posting a confirmed obligation instance creates a balanced journal entry and transitions instance to `posted`.
- `ObligationPostingService.postToLedger` orchestrates: validate → build JE command → post → transition.
- `FiscalPeriodRepository.findByDate` added (with `ORDER BY start_date LIMIT 1`).
- `PostToLedgerCommand` and `PostToLedgerResult` types in Ops.fs.
- 15 new tests, 15 Gherkin scenarios. PR #18 merged.
- Reviewer findings fixed: `LIMIT 1` on findByDate, rollback on read-phase error paths.

### Project 019 — Create and Confirm Transfers shipped
- Two operations: initiate (in-flight record, no JE) and confirm (post JE, update transfer).
- `TransferRepository` (insert, findById, updateConfirm) and `TransferService` (initiate, confirm).
- Asset-type validation at initiation: both accounts must be active asset accounts.
- Confirm follows P018's three-phase pattern (read+validate → JE post → transfer update).
- `TransferStatus` module, `InitiateTransferCommand`, `ConfirmTransferCommand` in Ops.fs.
- 14 new tests, 14 Gherkin scenarios. PR #19 merged.
- Reviewer finding fixed: `txn.Commit()` on read-phase happy path for consistency with P018.

### Orphaned test data incident
- P018's `@FT-POL-013` (closed fiscal period) was failing because an orphaned fiscal period from a prior session covered the same date range. Cleaned up manually. Tests use unique date ranges (2099) to avoid recurrence in P019.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Critical path continues
- **020** — Invoice Readiness. Next up.
  - Checks whether all obligations for a tenant/period are posted and ready to generate an invoice.
  - See `Projects/BACKLOG/020-invoice-readiness.md`.

### Design decisions carrying forward
- Non-atomic cross-service orchestration accepted (P018, P019). JE post + entity update are separate transactions. Retry is safe.
- Both obligation posting and transfer confirmation use debit=dest/to, credit=source/from. Account types encode semantics.
- `FiscalPeriodRepository.findByDate` returns first matching period (LIMIT 1, ordered by start_date). Periods should not overlap.

### Stale branches
All cleaned up. No stale branches remain.

### Project 007 paperwork gap
Still no test-results.md. Still low priority.

## Current state

### Branch: main
HEAD: `43eaa32`
Working tree: clean (except wakeup files)

### Test suite
403 tests, all passing, zero warnings.

### Key files
- Workflow: `BdsNotes/workflow.md`
- Data model spec: `Projects/Project001-Database/DataModelSpec.md`
- Backlog: `Projects/BACKLOG/index.md`
- P018 plan: `Projects/Project018-PostObligationToLedger/Project018-plan.md`
- P019 plan: `Projects/Project019-Transfers/Project019-plan.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Log output: `/workspace/application_logs/leobloom/`

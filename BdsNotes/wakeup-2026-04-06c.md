# Wakeup — 2026-04-06c

## What happened this session

### P037 — CLI Reporting Commands (done, merged)

Full pipeline run: PO -> Planner -> Gherkin -> Builder -> QE -> Reviewer -> Governor -> PO signoff -> RTE merge.

- 5 new report commands: `trial-balance`, `balance-sheet`, `income-statement`, `pnl-subtree`, `account-balance`
- Thin wrappers around existing Ledger services, no new domain logic
- GAAP-compliant human-readable formatting (separate debit/credit on TB, A=L+E on BS, retained earnings breakout, etc.)
- Per-command `--json` flag. Removed top-level `--json` short-circuit for report commands.
- PO caught pnl-subtree argument mismatch: backlog said `--from/--to` but service only supports `--period`. Resolved in plan.
- 37 new tests, 725/725 passing at time of merge
- **PR #35**, merged to main

### P041 — CLI Account + Period Commands (done, merged)

Full pipeline run.

- 2 new subcommand groups: `account` (list, show, balance) and `period` (list, close, reopen, create)
- NOT a pure wrapper — required 5 new repo/service functions (account list, account show, period list, period create, period find-by-key)
- Close/reopen/balance delegated to existing P007/P009 services
- PO flagged the service gap early; Planner phased it correctly (repo/service first, CLI second)
- 54 new tests, 779/779 passing at time of merge
- **PR #36**, merged to main

### Bugfix — Account code resolution (direct to main)

- Dan caught that `AccountCommands.fs` was using `Int32.TryParse` to decide id-vs-code for account lookups. Account codes are strings that happen to be numeric ("1110"). This meant `account show 1110` looked up DB id 1110, not code "1110".
- Fixed: now tries code lookup first, falls back to ID only if code not found and input parses as int.
- `report account-balance` and `report pnl-subtree` were already correct (treated account arg as code string).
- **Commit `a9c6c6d`**, direct to main

### Backlog cleanup

- PO moved 30 done/cancelled backlog spec files to `Projects/BACKLOG/Done/`
- Active items remaining in main BACKLOG/ dir: P022, P035, P038, P056
- index.md updated

### New backlog items

- **P035** — Orphaned Posting Detection. PO wrote the spec from session transcript context. Diagnostic CLI command, read-only, low priority. P043 already prevents new orphans.
- **P056** — Replace parent_code with parent_id in ledger.account. Tech debt, low priority.

### Nightshift conception

Design conversation with Dan about automating the BD pipeline. See `/workspace/Nightshift/docs/brainstorm.md` for the full writeup. This is a separate project, not a LeoBloom item.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

1. **Commit the backlog cleanup + new backlog items + wakeup files.** There's a pile of uncommitted changes (moved files, new specs, index update).
2. **P038** — CLI Obligation Commands. Last CLI wrapper project. Has a new `upcoming` service method (not just wrappers). Independent of everything else.
3. **P022, P035** — lower priority backlog items.

## Current state

### Branch: main
HEAD: `a9c6c6d`
Working tree: dirty — backlog cleanup (30 deleted files moved to Done/), new P035 and P056 specs, updated index.md, wakeup files. Needs a commit.

### Test suite
779 passed, 0 failed, 0 skipped.

### Backlog snapshot
- Done: P001-P019, P021, P029-P034, P036-P037, P039-P055
- Cancelled: P020, P023-P027
- Not started: P022, P028, P035, P038, P056

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`

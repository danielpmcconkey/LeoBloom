# Wakeup — 2026-04-12a

## What happened this session

### Pulled Hobson's upstream work
15 new Hobson wakeups (04-05 through 04-12), ADR-004, P079/P080 backlog
cards, production appsettings, procedures moved to NAS, import pipeline
plan updates, remediation stories moved to Done.

### Backlog reconciliation — massive
Nightshift delivered P056–P077 since last session. Moved 23 specs to Done/,
updated the entire index. P060 and P078 confirmed done (Hobson-only).
Active backlog reduced from 28 items to 5, now down to 2 (P022, P038).

### PO blueprint rewrite (Nightshift)
Old PO was a rubber stamp — both gates were procedural checklists with no
business intent validation. Rewrote with:
- Gate 1: "Will this solve the business problem?"
- Gate 2: "Has the business need been met?"
- Mandatory written artifacts at both gates (plan-review.md, signoff.md)
- Explicit rejection criteria

### LeoBloom DSWF addenda — 9 files
Created `DSWF/conventions.md` (shared naming rules, artifact inventory) plus
role-specific addenda for all 8 pipeline agents (po, governor, reviewer,
gherkin-writer, builder, planner, ba, rte). Enforces `ProjectNNN-ShortName`
naming convention and LeoBloom-specific domain guidance (GAAP, DataSource
patterns, test isolation).

### Project directory cleanup
6 dirs renamed from `P0XX-` to `Project0XX-` prefix. 8 artifact files
renamed similarly. Merged duplicate P056 dirs.

### Nightshift cards enqueued and completed
Enqueued cards 25 (P079), 26 (P080), 27 (P035). All three completed.
- P079: irregular cadence — unblocks Hobson's `obligation agreement list`
- P080: reporting data extracts — unblocks Hobson's report pipeline
- P035: orphaned posting detection — diagnostic CLI command

### Nightshift OutcomeParser bug fix
Gherkin writer was writing `notes` as a JSON array; `GetString()` blew up.
Fixed with `CoerceToString()` helper that handles string, array, null, and
other JSON types. Deployed as v0.1.2.

### Skills created
- `/db:nightshift` — Nightshift DB queries
- `/db:leobloom_dev` — LeoBloom dev DB queries

### Open item: backlog update automation
Nobody is updating the backlog index or moving specs to Done/ after projects
complete. RTE (Haiku) is too dumb. PO is the right candidate but we haven't
wired it up yet. Tabled for later.

## Current state

### LeoBloom — Branch: main
- HEAD: `8f5af3f` docs: reconcile backlog — P035, P079, P080 done
- Working tree: clean (minus .serena/ and logs/)
- Backlog: 2 active items (P022, P038)

### Nightshift — Branch: main
- HEAD: `0ac5024` (source) — uncommitted: OutcomeParser fix, version bump
  to 0.1.2, BACKLOG.md
- Engine binary: v0.1.2 at `/opt/nightshift/`
- Queue: empty (cards 25–27 complete)

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Immediate: flesh out P038 and P022 backlog specs

Both specs need a GAAP-minded requirements pass before they can be enqueued.
This is not a BA agent job — neither the BA nor the PO thinks like an
accountant. BD needs to put on the GAAP hat and work through these with Dan
present for judgment calls.

**P038 (CLI Obligation Commands):**
- Current spec is just a command list with args. No acceptance criteria, no
  edge cases, no error handling.
- The `upcoming` query is the only new service method — everything else wraps
  existing services.
- Open questions: what do the COYS agents actually need from these commands?
  What's the error behavior for invalid status transitions via CLI?

**P022 (Balance Projection):**
- Written as a brainstorm/brief, not a structured backlog item.
- No acceptance criteria, no scope boundaries, no behavioral vs structural
  separation.
- GAAP implications: projection logic touches obligation instances, transfers,
  variable-amount obligations. Needs accountant-level thinking about what
  "projected balance" means.
- Functionally benefits from P038 being done first (upcoming query feeds
  projections).

### Then: enqueue P038, P022, and a new card from Hobson

Dan will have Hobson provide a new backlog item. After the specs are solid,
enqueue all three in Nightshift.

### Nightshift repo needs a commit

The OutcomeParser fix and v0.1.2 bump are uncommitted.

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Nightshift DB: `172.18.0.1:5432`, user `nightshift`, db `nightshift`
- Nightshift engine: v0.1.2 at `/opt/nightshift/`

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-12a.md in the LeoBloom repo, then report back
```

# Wakeup — 2026-04-05f

## What happened this session

### GAAP assessment landed
Hobson ran four auditors (Ledger Tracer, Omission Hunter, GAAP Mapper, Slop Detector)
against projects 001-018. Engine is sound — zero arithmetic errors, 68% of scenarios
rated solid. But 3 critical gaps, 8 significant gaps, and 3 cleanup items.

Full reports in `HobsonsNotes/GaapAssessment/`. Remediation brief in
`BdsNotes/remediation-2026-04-05.md`.

### Project 034 created — GAAP Remediation
PO wrote 15 stories in 7 batches. Stories in `Projects/BACKLOG/remediation-stories.md`.
Project 035 (Orphaned Posting Detection) added to backlog as future work.

### S8 design decision resolved
Dan decided: accept + detect. "Posted" is historical fact — voided JE doesn't rewrite
ops history. No reverse-sync, no new status. Future diagnostic query (P035) surfaces
posted instances with voided JEs as "needs attention." Decision documented in the
remediation stories file and as a comment in PostObligationToLedger.feature.

### Batches 1-5 implemented and committed
- **Batch 1 (StatusTransitions):** 18 invalid transition tests, is_active=false bug
  fix on skipped, notes preservation assertion. Found real bug — repository wasn't
  setting `is_active = false` for skipped transitions.
- **Batch 2 (PostObligationToLedger):** Double-posting prevention, atomicity of failed
  posting, S8 design intent documented.
- **Batch 3 (Missing specs):** OpeningBalances.feature (14 scenarios) and
  SubtreePLReport.feature (12 scenarios). Test code already existed — just needed Gherkin.
- **Batch 4 (ObligationAgreementCrud):** source=dest rejection (new validation code in
  `ObligationAgreementService.validateAccountReferences`), inactive dest test.
- **Batch 5 (Reporting):** Income statement multi-period scoping, period-close side effects.

### Code changes (not just tests)
- `ObligationInstanceRepository.fs`: `is_active = false` in SET clause for skipped status
- `ObligationAgreementService.fs`: source=dest guard in `validateAccountReferences`

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Remaining P034 work
- **Batch 6 (REM-012):** Account-type validation for obligation agreements. This is the
  only story requiring new business logic. Needs design decision: which account types
  are valid for payable vs receivable? Hobson's suggestion: payable = asset source +
  expense dest, receivable = revenue source + asset dest. Depends on REM-008 (done).
- **Batch 7 (REM-013, REM-014):** Cleanup — CRUD persistence verification, remove/
  differentiate redundant scenarios. Low priority.

### After P034
- **Project 020 — Invoice Readiness** is the critical path. P034 doesn't block it.

## Current state

### Branch: project/034-gaap-remediation
HEAD: `898c2af`
Working tree: clean (except wakeup files)
Pushed to origin, no PR yet.

### Test suite
427 tests, all passing, zero warnings.

### Key files
- Remediation stories: `Projects/BACKLOG/remediation-stories.md`
- Workflow: `BdsNotes/workflow.md`
- GAAP synthesis: `HobsonsNotes/GaapAssessment/synthesis.md`
- Remediation brief: `BdsNotes/remediation-2026-04-05.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`

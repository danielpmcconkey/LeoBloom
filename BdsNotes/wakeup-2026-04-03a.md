# Wakeup — 2026-04-03a (Project 2 — Test Harness & Configuration)

## What happened since you were last here

Dan and Hobson merged your Project 1 work, then restructured the solution.
**The project layout has changed.** Read carefully before you start.

### Solution restructure (already merged to main)

The old project names (Ledger, Ops, Data) are gone. The solution is now:

| Project | Purpose |
|---|---|
| `LeoBloom.Domain` | Pure F# types — `Ledger.fs`, `Ops.fs`. No dependencies. |
| `LeoBloom.Dal` | Data access layer. Contains `ConnectionString.fs` — the shared resolver. |
| `LeoBloom.Dal.Tests` | **Your main target.** Structural constraint Gherkin tests go here. |
| `LeoBloom.Domain.Tests` | Business logic BDD tests. Scaffold only — not populated until Project 3. |
| `LeoBloom.Migrations` | Unchanged. Must be refactored to use `Dal.ConnectionString.resolve`. |
| `LeoBloom.Api` | Unchanged. References Domain + Dal. |

### ConnectionString resolver

Already built: `Src/LeoBloom.Dal/ConnectionString.fs`. It reads `LEOBLOOM_ENV`
(defaults to `Development` if unset), loads the matching `appsettings.{env}.json`,
and substitutes `LEOBLOOM_DB_PASSWORD` from the environment.

**Use this resolver everywhere. Do not duplicate config loading.** The Migrations
project currently has its own inline config — refactoring it to use
`Dal.ConnectionString.resolve` is a Project 2 deliverable.

## Your assignment: Project 2

Read `Projects/Project002-TestHarness.md` for the full BRD. Summary:

1. **Executable Gherkin tests** — TickSpec + xUnit in `LeoBloom.Dal.Tests`.
   Wire up the 68+ scenarios from `Specs/Acceptance/`. Tests run against
   `leobloom_dev` via the shared resolver. Clean up after themselves (rollback
   transactions or explicit DELETE).
2. **Refactor Migrations** to use `Dal.ConnectionString.resolve` — one resolver,
   one way to get a connection string.
3. **Domain.Tests scaffolding** — builds and runs (empty), ready for Project 3.
4. `dotnet test` from solution root: all green, zero warnings, no test data
   persists.

### Tech choice: TickSpec

Dan wants TickSpec (F# Gherkin runner) + xUnit. If you have a strong reason to
propose something else, raise it with Dan before committing. Do not switch
unilaterally.

## Migration review gate — NEW STANDING ORDER

**Every migration you write will be reviewed by Hobson before it is applied to
prod.** This is non-negotiable. The `claude` role already cannot touch
`leobloom_prod`, so this is a second layer.

What this means for you:
- Write your migrations as normal against `leobloom_dev`.
- Do not put real account names, real tenant names, or any prod-specific data
  in migration files. Dev seed data uses the anonymized `SampleCOA.md` names.
- If a migration touches seed data or lookup table values, flag it clearly in
  your PR description so Hobson can review it with appropriate suspicion.

## Environment reminder

- .NET 10: `/home/sandbox/.dotnet` (add to PATH)
- `LEOBLOOM_ENV=Development` (set it if not already)
- `LEOBLOOM_DB_PASSWORD=claude`
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- DB: `leobloom_dev` on 172.18.0.1:5432, role `claude`

## What to do this session

1. Pull main — you need the restructure changes.
2. Read `Projects/Project002-TestHarness.md`.
3. Read `Src/LeoBloom.Dal/ConnectionString.fs` — understand the resolver.
4. Build it. Feature branch, PR when done.

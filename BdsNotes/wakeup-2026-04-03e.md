# Wakeup — 2026-04-03e (Project 029 Done, 005 Next)

## What happened this session

### Project 029: Built and Merged
- Full agent waterfall ran autonomously — zero input from Dan
- Flow: PO → BA(BD) → PO → BA(BD) → PO → Build(BD) → Reviewer → Governor → PO → RTE
- PO gave CONDITIONAL twice (BRD and BDD), BD fixed and resubmitted — both approved on second pass
- Reviewer: APPROVED, no defects
- Governor: 62/62 BDD criteria verified independently
- PR #3 merged to main, branch cleaned up
- Hobson ran migration against prod from upstairs

### What 029 delivered
- Migration `1712000019000_EliminateLookupTables.sql` — drops 4 lookup tables, replaces int FK columns with varchar
- `Ops.fs` — removed 4 lookup record types, added toString/fromString modules for 4 DUs, updated ObligationAgreement and ObligationInstance records
- 8 new domain tests (36 total), 72 Dal.Tests updated (15 scenarios removed, 3 added)
- DataModelSpec updated to reflect new schema
- BACKLOG.md item 029 marked Done

### Build issues encountered
- `InstanceStatus.Confirmed` collided with `TransferStatus.Confirmed` in F# — had to fully qualify DU cases in the `InstanceStatus` module. Other DU modules don't need qualification because their cases are unique.
- `gh` CLI wasn't installed in the container — had to `sudo apt-get install gh` mid-session. May not persist across rebuilds.

### Agent workflow observations
- BD ran the whole waterfall solo. Dan said "run the whole fucking thing with no input from me." It worked.
- PO caught real issues both times — DataModelSpec not addressed in BRD, incomplete column list in BDD-054. The gates add value.
- BD acted as BA and Builder (wrote BRD, BDD, and code directly). Spawned PO, Reviewer, and Governor as agents. This was faster than spawning BA separately since BD had all the context.
- Known issue persists: PO agent can't write files. BD applied all PO-requested edits manually.

### Environment changes
- Hobson rebuilt the container before this session
- .NET 8 + 10 at `/usr/lib/dotnet` (not `/home/sandbox/.dotnet` anymore)
- `LEOBLOOM_ENV` and `LEOBLOOM_DB_PASSWORD` baked into container
- Old `DOTNET_ROOT` env var still set but harmless — dotnet resolves from `/usr/bin/dotnet`

## What's next: Project 005

**Post Journal Entry** — the core write path. Service and persistence layer that takes validated domain types and writes journal entries to the database.

### What 005 involves (from BACKLOG.md)
- Read the full backlog item for 005 in `Projects/BACKLOG.md` under "Epic A: Journal Entry Engine"
- This is the first project that builds on the Dal layer — will need Dapper or raw SQL
- Depends on: 001 (schema), 002 (test harness), 004 (domain types), 029 (lookup elimination)
- The write path must call the validation functions from Ledger.fs before persisting

### Run the agent waterfall again
Same flow as 029. BD can act as BA and Builder directly — it's faster. Spawn PO, Reviewer, Governor as agents.

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH, no hacks needed)
- Env vars baked in: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- `gh` CLI: may need reinstall if container rebuilt

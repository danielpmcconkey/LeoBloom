# Wakeup — 2026-04-08a

## What happened this session

### Nightshift completed all 12 cards overnight

P073 through P072, all merged to main. Full batch ran from ~11:49pm to ~5:03am.

| Card | Project | Notes |
|------|---------|-------|
| 9 | P073 — Connection Injection | 6 builder attempts (4 timeouts at 30min, 1 hung, 1 success at 90min). The big refactor — every service function now accepts NpgsqlTransaction. |
| 10 | P062 — Normal Balance Consolidation | Clean, ~20min |
| 11 | P068 — Fiscal Period Overlap Prevention | Clean, ~28min |
| 12 | P069 — Account CRUD Behavioral Specs | Clean, ~26min |
| 13 | P067 — Portfolio Validation Gaps | Clean, ~14min |
| 14 | P063 — Portfolio Delete Restriction Tests | Clean, ~12min |
| 15 | P064 — Balance Sheet Equation Verification | Clean, ~11min |
| 16 | P065 — Balance Projection Filter Tests | Clean, ~23min |
| 17 | P066 — Transfer Atomicity Tests | Clean, ~95min (slow but succeeded) |
| 18 | P070 — Missing Portfolio CLI Commands | Clean, ~27min |
| 19 | P071 — CLI parseDate Consolidation | Clean, ~24min |
| 20 | P072 — Housekeeping Batch | Clean, ~34min |

### Test suite status

1041 tests, all passing. 10 consecutive clean parallel runs after the overnight work settled. Early runs after checkout had 1-2 failures from residual dirty data left by P073's builder timeout thrashing — not an ongoing issue.

P073's connection injection pattern is working: services accept NpgsqlTransaction, tests use rollback-only transactions, no cleanup infrastructure needed. The PortfolioTracker/TestCleanup pattern has been removed.

### Timeout fix

Builder step was timing out at 30 minutes (the `DefaultTimeoutSeconds` in appsettings.json was dead config — actual timeout comes from `nightshift.workflow_step.timeout_seconds` in the DB). Updated builder, qe, and builder_response to 5400s (90 min) in the DB. Also updated appsettings.json in source and published binary, but that value isn't actually read.

### Backlog index still stale

Hobson updated the index with 062–072 but everything still shows "Not started" — P022 through P072 are all done now. Needs a full reconciliation pass.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

1. **Reconcile backlog index** — mark P022, P035, P038, P056–P073 as Done. Move spec files to Done/.
2. **P060** — Hobson's data migration. Still not a Nightshift card. Only remaining not-started item.
3. **Nightshift timeout strategy** — static timeouts are a band-aid. Need progress detection: kill agents that stop producing output, let active agents run. This is a Nightshift engine feature. Dan wants it prioritized.
4. **Nightshift dogfooding** — Dan still needs to review the 4 retroactive BDD spec files before the dogfooding plan proceeds.
5. **Run another GAAP audit?** — 12 cards worth of new code since the last one. Might be worth a sweep.

## Current state

### Branch: main
- HEAD: `c339583` feat: housekeeping batch — deps, CLI consistency, repo conventions (P072)
- Working tree: clean

### Nightshift queue
- Empty. All 20 cards complete. Engine idle-polling.

### Test suite
- 1041 tests, 0 failures, stable across 10+ consecutive parallel runs

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Nightshift DB: `172.18.0.1:5432`, user `nightshift`, db `nightshift`

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-08a.md in the LeoBloom repo and report back
```

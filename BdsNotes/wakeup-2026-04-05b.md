# Wakeup — 2026-04-05b

## What happened this session

### Project 014 — Obligation Agreements (CRUD) shipped
- Full CRUD service for `ops.obligation_agreement`: create, getById, list (with filters), update, deactivate.
- Command types (`CreateObligationAgreementCommand`, `UpdateObligationAgreementCommand`) and pure validation module added to `Ops.fs`.
- `ObligationAgreementRepository.fs` + `ObligationAgreementService.fs` created in `LeoBloom.Utilities`.
- Deactivation guard checks for active obligation instances before allowing deactivate.
- DB validation: source/dest account FKs must exist and be active.
- 31 new tests, 28 Gherkin scenarios. PR #14 merged.
- Reviewer findings fixed: O(n^2) list building (cons+rev), weak empty-list test, missing entry logs on getById/list.

### Project 015 — Spawn Obligation Instances shipped
- `ObligationInstanceService.spawn`: given agreement ID + date range, generates instances based on cadence.
- Pure date generation logic in `ObligationInstanceSpawning` module (Ops.fs): Monthly, Quarterly (calendar quarters), Annual (startDate anchors month), OneTime.
- Day clamping (e.g., expectedDay=31 in Feb becomes 28/29). Defaults to 1 when expectedDay is null.
- Name generation: "Jan 2026", "Q1 2026", "2026", "One-time" by cadence.
- Skip-not-error on duplicate dates (idempotent re-spawn for overlapping ranges). Returns `SpawnResult` with created list + skippedCount.
- Amount pre-filled from agreement for fixed; None for variable.
- All instances start as `Expected`, `isActive=true`.
- `findExistingDates` ignores `is_active` — soft-deleted instances still occupy their date slot.
- `ObligationInstanceRepository.fs` + `ObligationInstanceService.fs` created.
- 25 new tests, 18 Gherkin scenarios. PR #15 merged.
- Reviewer finding fixed: locale-dependent `ToString("MMM yyyy")` — now uses `CultureInfo.InvariantCulture`.

### PO conditional on P015 plan — 5 fixes applied
- Clarified quarterly date generation (expectedDay of Jan/Apr/Jul/Oct, calendar quarters)
- Added design decisions: no DB unique constraint (app-enforced), findExistingDates ignores is_active
- Tagged acceptance criteria as behavioral vs structural
- Specified new test helper (don't modify existing)

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Critical path continues: obligation lifecycle
- **016** — Status Transitions (state machine). Next up.
  - Status lifecycle: expected -> in_flight -> confirmed -> posted, with overdue and skipped branches.
  - DataModelSpec invariants: journal_entry_id only when posted, amount required when confirmed/posted.
  - This is where the state machine logic lives.
- **017** — Overdue Detection
- **018** — Post Obligation to Ledger (convergence point: ops meets ledger)

### Design decisions carrying forward
- Annual cadence uses startDate's month as anchor. If this becomes a problem, add `expectedMonth` to agreement.
- Calendar quarters (Jan/Apr/Jul/Oct), not fiscal. Revisit if fiscal year doesn't start in January.
- No DB unique constraint on `(obligation_agreement_id, expected_date)` — app-enforced only.

### Stale branches
All cleaned up. No stale branches remain.

### Project 007 paperwork gap
Still no test-results.md. Still low priority.

## Current state

### Branch: main
HEAD: `1dac0c2`
Working tree: clean

### Test suite
341 tests, all passing, zero warnings.

### Key files
- Workflow: `BdsNotes/workflow.md`
- Data model spec: `Projects/Project001-Database/DataModelSpec.md`
- Backlog: `Projects/BACKLOG/index.md`
- P014 plan: `Projects/Project014-ObligationAgreements/Project014-plan.md`
- P015 plan: `Projects/Project015-SpawnObligationInstances/Project015-plan.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Log output: `/workspace/application_logs/leobloom/`

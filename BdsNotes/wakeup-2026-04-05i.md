# Wakeup — 2026-04-05i

## What happened this session

### P043 — Idempotency Guards (shipped)
Full agent pipeline: PO → Plan → Gherkin Writer → Builder → QE → Reviewer → Governor → PO signoff → RTE. PR #20 merged.

Added `findNonVoidedByReference` to `JournalEntryRepository.fs`. Guards inserted in `ObligationPostingService.postToLedger` and `TransferService.confirm` — before posting a journal entry, check for an existing non-voided entry by reference_type/reference_value. If found, skip the post and use the existing ID for the status transition. 4 new tests (428 total). Addresses audit Tier 1 Finding #1.

### P044 — Database Indexes (shipped)
Full pipeline (abbreviated — no Gherkin/QE needed for DDL-only). PR #21 merged.

One migration: `1712000020000_AddSecondaryIndexes.sql` with 6 indexes on FK columns (journal_entry_line, journal_entry, obligation_instance). Addresses audit Tier 1 Finding #2.

### Workflow updates
- Added step 15 to `BdsNotes/workflow.md`: stop after RTE and await Dan's instruction before starting next project.
- Updated wakeup protocol memory: read workflow.md BEFORE reporting back.
- New feedback memory: stop between projects — Dan evaluates context health at project boundaries.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Remediation queue (in order per backlog sequencing notes)
1. **P045 — Domain-based project reorg** — structural prerequisite for CLI. Move domain-specific services/repos from Utilities into LeoBloom.Ledger and LeoBloom.Ops. This is the big one.
2. **P046 — Delete LeoBloom.Api** — dead code, trivial.
3. **P047 — Delete ghost directories** — dead scaffolding, trivial.
4. **P048 — Test cleanup** — nuke dead tests per audit decisions.
5. **P049-P051** — small fixes, any order.

Then the CLI track starts at P036.

## Current state

### Branch: main
HEAD: `06ba0a1`
Working tree: untracked wakeup files only (a through h)
Pushed to origin: yes

### Test suite
428 tests, all passing.

### Backlog snapshot
- Done: P001-P019, P029-P034, P043, P044
- Cancelled: P020, P023-P027
- Not started: P021, P022, P035-P042, P045-P051

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- 6 new indexes live in leobloom_dev (migration already applied)

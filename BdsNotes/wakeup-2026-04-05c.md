# Wakeup — 2026-04-05c

## What happened this session

### Project 016 — Status Transitions shipped
- State machine for obligation instance lifecycle: expected → in_flight → confirmed → posted, with overdue and skipped branches.
- `TransitionCommand` type and `StatusTransition` module added to `Ops.fs`.
- Pure validation: instanceId > 0, journal_entry_id required for posted, confirmed_date required for confirmed.
- DB guards: instance must exist and be active, current→target must be in allowed transitions map, journal entry must exist for posted.
- Field-gated updates: amount only written on confirmed, journal_entry_id only on posted, confirmed_date only on confirmed, notes only on skipped. Mirrors the `amountToSet` pattern.
- `findById` and `updateStatus` added to `ObligationInstanceRepository.fs`.
- `transition` function added to `ObligationInstanceService.fs` (with `journalEntryExists` helper).
- `insertObligationInstanceFull` test helper added for creating instances in arbitrary states.
- 22 new tests, 22 Gherkin scenarios. PR #16 merged.
- Reviewer finding fixed: field leakage — journalEntryId, confirmedDate, and notes were passed through to DB unconditionally on all transitions. Now gated by target status.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Critical path continues: obligation lifecycle
- **017** — Overdue Detection. Next up.
  - Batch operation: find all active instances where status = expected and expected_date < today, transition them to overdue.
  - This is the automation layer on top of the manual overdue transition built in 016.
- **018** — Post Obligation to Ledger (convergence point: ops meets ledger)
  - Creates a journal entry from the obligation instance's agreement (source/dest accounts, amount) and transitions to posted.

### Design decisions carrying forward
- Annual cadence uses startDate's month as anchor.
- Calendar quarters (Jan/Apr/Jul/Oct), not fiscal.
- No DB unique constraint on `(obligation_agreement_id, expected_date)` — app-enforced only.
- Field updates are status-gated: each field only written when transitioning to the status that owns it.

### Stale branches
All cleaned up. No stale branches remain.

### Project 007 paperwork gap
Still no test-results.md. Still low priority.

## Current state

### Branch: main
HEAD: `1bc7047`
Working tree: clean (except wakeup files)

### Test suite
363 tests, all passing, zero warnings.

### Key files
- Workflow: `BdsNotes/workflow.md`
- Data model spec: `Projects/Project001-Database/DataModelSpec.md`
- Backlog: `Projects/BACKLOG/index.md`
- P016 plan: `Projects/Project016-StatusTransitions/Project016-plan.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Log output: `/workspace/application_logs/leobloom/`

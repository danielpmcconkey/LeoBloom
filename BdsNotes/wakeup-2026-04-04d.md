# Wakeup — 2026-04-04d

## What happened this session

### Merged Project 030 to main
Branch `refactor/030-unfuck-test-harness` merged and pushed. PR #6 closed.

### Built Project 032 — far beyond original scope
Original scope was "write a test author agent blueprint." What we actually
built was a dev shop workflow overhaul:

- **DSWF (Dan's Scaled Waterfall):** Per-project directory where agents
  read role-specific addenda before starting work. `DSWF/{role}.md`.
- **QE addendum** (`DSWF/qe.md`): All institutional test knowledge from
  Project 030 — connection patterns, cleanup discipline, isolation strategy,
  anti-patterns.
- **Rewrote all 7 agent blueprints** in `~/.claude/agents/` (symlinked to
  ai-dev-playbook). Stripped CE command dependencies. Added DSWF checks.
- **New agents:** Gherkin Writer (writes .feature files from plans) and
  QE (writes test implementations after Builder).
- **Rewrote BA** from BRD-writer to Brainstorm Analyst (CE-inspired
  conversational discovery).
- **Rewrote Planner** with integrated research + deepen, no CE dependency.
- **New pipeline:** PO → Brainstorm → Plan/Deepen → Gherkin Writer →
  Builder → QE → Reviewer → Governor → PO signoff → RTE
- **BdsNotes/workflow.md** — the orchestrator doc, replaces old project flow.
- **Projects/Workflow.md → Projects/RACI.md** — renamed to avoid confusion,
  trimmed to roles only.
- **Wakeup protocol** added to BD's MEMORY.md.

### Updated backlog
030 and 032 marked Done. 033 added to status table.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Project 033 — Seal DataSource.connectionString
`DataSource.connectionString` is public so Migrations can pass a raw string
to Migrondi. Should be private. Investigate whether Migrondi can accept an
NpgsqlDataSource/NpgsqlConnection, or whether Migrations should build its
own string. Small, contained fix.

### Project 031 — Foundational Logging
Needs a design discussion. Every project going forward needs it. QE addendum
currently notes `eprintfn` as placeholder.

### Shakedown
Use the new workflow on 033 as a test run. Small project, low stakes.

## Current state

### Branch: main
HEAD: `97456e4`
Uncommitted changes: none

### Key files
- Agent blueprints: `~/.claude/agents/` (symlinks to ai-dev-playbook)
- DSWF: `DSWF/qe.md`
- Workflow: `BdsNotes/workflow.md`
- Backlog: `Projects/BACKLOG.md`
- RACI: `Projects/RACI.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`

# Wakeup — 2026-04-05k

## What happened this session

### P049 — Consolidate Helper Duplication (shipped)
Full agent pipeline. PR #26 merged. Consolidated three groups of duplicated helpers:
- **optParam** (4 copies) → single `DataHelpers.fs` in LeoBloom.Utilities, `obj option` signature
- **repoRoot** (2 copies) → `RepoPath` module in TestHelpers.fs, uses `__SOURCE_DIRECTORY__`
- **Constraint test helpers** (2 copies) → `ConstraintAssert` module in TestHelpers.fs, Ledger's parameterized pattern
- Reviewer caught DeleteRestrictionTests.fs also had duplicates — fixed in-scope

### P036 — CLI Framework + Ledger Commands (shipped)
Full agent pipeline. PR #27 merged. Greenfield CLI project:
- **LeoBloom.CLI** — Argu-based console app with exit codes (0/1/2), output formatting (human + `--json`), error routing (stderr/stdout)
- **Three ledger commands:** `ledger post`, `ledger void`, `ledger show`
- Added `getEntry` read path to JournalEntryService/Repository (service gap)
- `--json` on both top-level and per-command DUs (Argu doesn't propagate top-level flags)
- Post-review fix: Serilog Console sink moved to stderr (was contaminating `--json` stdout)
- 40 new CLI tests, all passing

### Architecture & workflow changes
- **ADR reorganization:** Moved ADR-001, ADR-002 from `BdsNotes/decisions/` to `Documentation/ADR/`. Created ADR-003 (CLI Architecture).
- **ADR-003 — CLI Architecture:** CLI is parse→call→format→exit code. No business logic in CLI. Gherkin tests CLI behavior only, not service logic. Referenced by all P036-P042 backlog items.
- **Workflow reread step:** Added step 9 to `BdsNotes/workflow.md` — BD re-reads workflow.md after Builder returns, before spawning QE/Reviewer. Context checkpoint to prevent mid-session drift on agent spawning protocol.
- **Design session:** Confirmed services already own orchestration (connections, transactions, validation). CLI layer is thin. Argu chosen for arg parsing.

### Code audit remediation complete
P043-P051 all Done. Remediation block closed.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### CLI track (in order per wakeup-2026-04-05j)
1. **P038 — CLI Obligation Commands** — hardest CLI project, ~15 commands, new `upcoming` service method. Dan chose this as next to stress-test the CLI pattern.
2. P037, P039, P041 — thin CLI wrappers, any order
3. P021 → P042 — invoice persistence then CLI wrapper
4. P040 — CLI tax reports (new logic, target before 2027 tax season)
5. P035 — orphaned posting detection (standalone)
6. P022 — balance projection (lowest priority)

### Pending discussion
- Dan mentioned wanting to "do one of the hard ones" to establish the CLI pattern before the easy wrappers. P038 was BD's recommendation. Dan didn't explicitly confirm P038 vs P040 — check before starting.

## Current state

### Branch: main
HEAD: `dbda298`
Working tree: untracked BdsNotes files only (wakeups, workflow.md edits, deleted decisions dir)
Pushed to origin: yes

### Test suite
462 tests, 460 passing, 2 pre-existing failures (PostObligationToLedgerTests — closed fiscal period).

### Backlog snapshot
- Done: P001-P019, P029-P034, P036, P043-P051
- Cancelled: P020, P023-P027
- Not started: P021, P022, P035, P037-P042

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Dan ran prod migration this session — prod is current

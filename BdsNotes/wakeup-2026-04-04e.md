# Wakeup — 2026-04-04e

## What happened this session

### Four projects shipped
- **Project 033** — Seal DataSource internals. Migrations builds its own connection string, Dal reference removed. PR #7 merged.
- **Project 031** — Foundational logging + rename Dal to Utilities. Serilog with console + file sinks, thin F# module wrapper, wired into all services and test infrastructure. One log file per CLI execution. PR #8 merged.
- **Project 008** — Trial balance report. Period-scoped activity grouped by account type, integrity check, two entry points (by ID and period key). 16 tests. PR #9 merged.
- **Project 009** — Close/reopen fiscal period. Toggle `is_open`, reopen requires reason (logged). Idempotent. 15 tests. PR #10 merged.

### Workflow improvements
- **Gherkin Writer blueprint rewritten.** Old version produced 31 scenarios for 16 acceptance criteria (Project 031). New version distinguishes behavioral specs (Gherkin) from structural verification (QE-owned). Project 008 produced 11 behavioral scenarios — much tighter.
- **PO blueprint updated.** Now understands BDD context — Gherkin is the behavioral contract, structural criteria are verified separately by QE/Governor.
- **Backlog restructured.** Monolithic `BACKLOG.md` (2000 lines) split into `Projects/BACKLOG/` directory: `index.md` (priority table + sequencing notes) + one file per not-done project. Done items keep their index row but file gets deleted.

### Design decisions made
- LeoBloom's "API" will be a CLI, not a web API.
- Serilog for logging. Console + file sinks. No enrichers, no aggregation, no Debug level.
- Log files: one per CLI execution, `leobloom-yyyyMMdd.HH.mm.ss.log`, base dir configurable in appsettings (`/workspace/application_logs/leobloom/`).
- `LeoBloom.Dal` renamed to `LeoBloom.Utilities` — the odd sock drawer. DataSource, logging, services all live here.
- Migrations stays cowboy (`printfn`). It owns its own DB access. Nobody touches it.
- Project 028 confirmed as "done by prior work" (005/006 integration tests already cover write-level validation). Hobson independently validated.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Critical path
`008 → 011/012 → 013`. Next up is **011 (Income Statement)** and **012 (Balance Sheet)** — these can run in parallel. Both depend on 008 (done).

### 010 — Opening Balances
Also unblocked (depends on 005, done). Independent of the critical path but useful for completeness.

### Cleanup candidates
- Stale local branches: `feat/project1-database`, `feature/031-foundational-logging`, `refactor/030-unfuck-test-harness`, `refactor/033-seal-datasource-internals`. Safe to delete.
- Project 007 never formally closed (no test-results.md, BDD doc says "Awaiting PO Approval"). Code is on main and tested. Paperwork gap only.

## Current state

### Branch: main
HEAD: `41d3d7d`
Uncommitted changes: none (wakeup file will be uncommitted)

### Test suite
218 tests, all passing, zero warnings.

### Key files
- Agent blueprints: `~/.claude/agents/` (symlinks to ai-dev-playbook)
- DSWF: `DSWF/qe.md`
- Workflow: `BdsNotes/workflow.md`
- Backlog index: `Projects/BACKLOG/index.md`
- RACI: `Projects/RACI.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Log output: `/workspace/application_logs/leobloom/`

# Wakeup — 2026-04-05a

## What happened this session

### Project 013 — P&L by Account Subtree shipped
- Recursive CTE walks `parent_code` to resolve account hierarchies, filters IS query to subtree.
- Reuses `IncomeStatementLine`/`IncomeStatementSection` types. New `SubtreePLReport` type adds root account context.
- Dual entry points (period ID and period key). 15 new tests.
- PR #12 merged.

### Project 010 — Opening Balances shipped
- Convenience service: takes account/balance pairs, looks up normal balance direction, auto-computes balancing equity entry, delegates to `JournalEntryService.post`.
- New types: `OpeningBalanceEntry`, `PostOpeningBalancesCommand`.
- Validates: no duplicates, positive balances, equity-type balancing account required.
- 16 new tests. PR #13 merged.

### API projects cancelled
- Projects 023–027 (all endpoint projects) marked Cancelled in backlog.
- Dan's direction: consumption layer will be CLI, not REST API.
- Sequencing notes updated accordingly.

### Test helper enhancement
- `InsertHelpers.insertAccountWithParent` added to TestHelpers for building account hierarchies in tests.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Critical path: obligation lifecycle
The ledger engine is complete (001–013 all done). Next is the obligation track:
- **014** — Obligation Agreements (CRUD for contract terms). Foundation for everything else.
- **015** — Spawn Obligation Instances
- **016** — Status Transitions (state machine)
- **017** — Overdue Detection
- **018** — Post Obligation to Ledger (convergence: ops meets ledger)

This is the `ops` schema — different patterns from ledger. CRUD + state machines instead of append-only + reporting. Read `DataModelSpec.md` sections on `ops.obligation_agreement` and `ops.obligation_instance` before planning 014.

### Stale branches
Still safe to delete: `feat/project1-database`, `feature/031-foundational-logging`, `refactor/030-unfuck-test-harness`, `refactor/033-seal-datasource-internals`.

### Project 007 paperwork gap
Still no test-results.md. Code is on main and tested. Low priority.

## Current state

### Branch: main
HEAD: `bd2f4ee`
Working tree: clean

### Test suite
285 tests, all passing, zero warnings.

### Key files
- Workflow: `BdsNotes/workflow.md`
- Data model spec: `Projects/Project001-Database/DataModelSpec.md` (ops schema details)
- Backlog: `Projects/BACKLOG/index.md`
- RACI: `Projects/RACI.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Log output: `/workspace/application_logs/leobloom/`

# Wakeup — 2026-04-05d

## What happened this session

### Project 016 — Status Transitions shipped
- State machine for obligation instance lifecycle: expected → in_flight → confirmed → posted, with overdue and skipped branches.
- `TransitionCommand` type and `StatusTransition` module in `Ops.fs`.
- Field-gated updates: amount only on confirmed, journal_entry_id only on posted, confirmed_date only on confirmed, notes only on skipped.
- `findById` and `updateStatus` in repository. `transition` function in service.
- 22 new tests, 22 Gherkin scenarios. PR #16 merged.
- Reviewer finding fixed: field leakage on non-target transitions.

### Project 017 — Overdue Detection shipped
- Batch sweep: finds active `expected` instances with `expected_date < referenceDate`, transitions each to `overdue` via P016 machinery.
- `OverdueDetectionResult` type with `transitioned`, `errors`, `queryFailed`.
- `findOverdueCandidates` in repository. `detectOverdue` in service.
- Reference date parameterized (not hardcoded today). Idempotent.
- 11 new tests, 11 Gherkin scenarios. PR #17 merged.
- Reviewer finding fixed: added `queryFailed` flag to distinguish "nothing to do" from "query failed."
- Reviewer finding acknowledged: test isolation concern with global queries. Accepted — batch queries are inherently global.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Critical path continues: convergence point
- **018** — Post Obligation to Ledger. Next up.
  - This is the convergence point: ops meets ledger.
  - Creates a journal entry from the obligation instance's agreement (source/dest accounts, amount).
  - Transitions instance to `posted` with the new journal entry ID.
  - Requires: instance in `confirmed` status, amount set, agreement has source/dest accounts.

### Design decisions carrying forward
- Annual cadence uses startDate's month as anchor.
- Calendar quarters (Jan/Apr/Jul/Oct), not fiscal.
- No DB unique constraint on `(obligation_agreement_id, expected_date)` — app-enforced only.
- Field updates are status-gated: each field only written when transitioning to the status that owns it.
- Overdue detection only targets `expected` status, not `in_flight`.

### Stale branches
All cleaned up. No stale branches remain.

### Project 007 paperwork gap
Still no test-results.md. Still low priority.

## Current state

### Branch: main
HEAD: `7f9cf2f`
Working tree: clean (except wakeup files)

### Test suite
374 tests, all passing, zero warnings.

### Key files
- Workflow: `BdsNotes/workflow.md`
- Data model spec: `Projects/Project001-Database/DataModelSpec.md`
- Backlog: `Projects/BACKLOG/index.md`
- P016 plan: `Projects/Project016-StatusTransitions/Project016-plan.md`
- P017 plan: `Projects/Project017-OverdueDetection/Project017-plan.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Log output: `/workspace/application_logs/leobloom/`

# Wakeup — 2026-04-04f

## What happened this session

### Projects 011 + 012 shipped together
- **Project 011** — Income Statement. Period-scoped revenue minus expenses, net income, dual entry points (by ID and period key). 16 behavioral scenarios, 5 structural tests.
- **Project 012** — Balance Sheet. Cumulative point-in-time snapshot: assets, liabilities, equity, computed retained earnings, accounting equation check (`isBalanced`). 11 behavioral scenarios, 4 structural tests.
- PR #11 merged.

### Key design decisions
- Built 011 and 012 as one project — Balance Sheet retained earnings is computed from the same revenue/expense data the Income Statement reports on.
- **GAAP-first**: Dan established that LeoBloom targets AccPac/QuickBooks-level correctness. Zero-balance accounts with activity must display. Retained earnings computed in F#, not via closing entries or SQL gymnastics.
- Balance Sheet is date-based (`asOfDate: DateOnly`), not period-based. No `getByPeriodKey` entry point — balance sheets don't belong to fiscal periods.
- Separate domain types for IS and BS lines even though structurally identical today (they'll diverge).
- Income Statement reuses `TrialBalanceRepository.resolvePeriodId`/`periodExists` for period lookups.

### Reviewer findings (all fixed)
- Balance Sheet tests needed direct `retainedEarnings` assertions (not just `isBalanced`). Fixed with far-past dates (1950s) to isolate from parallel test pollution.
- "Before any entries" test needed `DateOnly(1900, 1, 1)` to actually assert all zeros.
- IS `getByPeriodKey` had redundant double-lookup — removed.
- Dead `postMultiLineEntry` helper in IS tests — removed.

### Workflow update
- Added GAAP Compliance section to `BdsNotes/workflow.md`. Standing order: evaluate every design decision against GAAP first, convenience second.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work.

### Critical path
`011/012 → 013`. Next up is **Project 013 (P&L by Account Subtree)** — the filtered version of the income statement grouped by parent account hierarchy.

### 010 — Opening Balances
Still unblocked (depends on 005, done). Independent of the critical path.

### Cleanup candidates
- Stale local branches from prior sessions: `feat/project1-database`, `feature/031-foundational-logging`, `refactor/030-unfuck-test-harness`, `refactor/033-seal-datasource-internals`. Safe to delete.
- Project 007 still has a paperwork gap (no test-results.md). Code is on main and tested.

## Current state

### Branch: main
HEAD: `b0e6bae`
Uncommitted changes: this wakeup file + the previous wakeup (`wakeup-2026-04-04e.md`)

### Test suite
254 tests, all passing, zero warnings.

### Key files
- Agent blueprints: `~/.claude/agents/` (symlinks to ai-dev-playbook)
- DSWF: `DSWF/qe.md`
- Workflow: `BdsNotes/workflow.md`
- Backlog index: `Projects/BACKLOG/index.md`
- RACI: `Projects/RACI.md`

## Environment
- .NET 10: `/usr/lib/dotnet` (on PATH)
- Env vars: `LEOBLOOM_ENV=Development`, `LEOBLOOM_DB_PASSWORD` set
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `claude`, db `leobloom_dev`
- Log output: `/workspace/application_logs/leobloom/`
